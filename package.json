{
  "name": "nexus-schedule",
  "version": "1.0.0",
  "description": "Intelligent Voice-Powered Appointment Booking System with Agentic AI",
  "main": "backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd backend && npm test",
    "install-deps": "cd backend && npm install && cd ../frontend && npm install && cd ../ai-agent && pip install -r requirements.txt"
  },
  "keywords": ["ai", "scheduling", "voice", "appointment", "agentic"],
  "author": "NexusSchedule Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
